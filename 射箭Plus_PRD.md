# 射箭Plus - 产品需求文档（PRD）

## 1. 项目概述

### 1.1 产品定位
射箭Plus 是一款面向运动科学研究和专业射箭训练的 Windows 桌面应用，提供自定义关键点检测模型训练、实时人体姿态分析和3D运动追踪功能。

### 1.2 核心价值
- **降低门槛**：无需深度学习背景即可训练专业的关键点检测模型
- **专项优化**：针对射箭运动特定关键点（弓箭器械）的精准识别
- **实时反馈**：基于GPU加速的实时姿态分析，帧率可达30+ FPS
- **科研级精度**：支持多视角3D重建和专业生物力学数据导出

### 1.3 目标用户
- 运动科学研究人员
- 专业射箭教练与运动员
- 体育院校师生
- 运动康复机构

---

## 2. 技术架构

### 2.1 核心技术栈
| 模块 | 技术选型 | 理由 |
|------|---------|------|
| 深度学习框架 | MMPose + RTMO | 业界领先的姿态估计框架 |
| 推理引擎 | ONNX Runtime (GPU) | 跨平台、高性能推理 |
| UI框架 | PyQt6 / PySide6 | 成熟的桌面应用框架 |
| 图像处理 | OpenCV (CUDA 加速) | GPU加速的视频处理 |
| 3D计算 | OpenCV 相机标定 + NumPy | 立体视觉重建 |
| 数据处理 | Pandas + SciPy | 科学计算与滤波 |

### 2.2 系统要求
- **操作系统**：Windows 10/11 (64-bit)
- **GPU**：NVIDIA GPU with CUDA 11.8+（推荐 RTX 3060 以上）
- **内存**：16GB RAM（推荐 32GB）
- **存储**：20GB 可用空间
- **摄像头**：1080p USB 摄像头 × 2（双视角）

### 2.3 性能指标
- 推理速度：≥30 FPS（RTX 3060）
- 关键点定位误差：< 5 像素（2D）
- 模型训练时间：200张图片约 2-4 小时（取决于GPU）
- 启动时间：< 3 秒

---

## 3. 功能设计

### 3.1 工作流架构（左侧功能栏）

```
┌─────────────────┐
│  ① 导入素材      │ → 视频抽帧、图像管理
├─────────────────┤
│  ② 标注图片      │ → 自动预标注 + 手动精修
├─────────────────┤
│  ③ 训练模型      │ → 数据集准备、模型微调
├─────────────────┤
│  ④ 应用模型      │ → 实时推理、坐标追踪
├─────────────────┤
│  ⑤ 导出数据      │ → 滤波、格式转换、3D重建
└─────────────────┘
```

---

### 3.2 模块详细设计

#### 📦 模块一：导入素材

**功能目标**：将视频高效转换为训练用图像序列

**输入**
- 视频文件（MP4/AVI，支持拖拽）
- 抽帧间隔（默认每5帧，可调整为 1-30 帧）

**处理流程**
1. 视频解码（使用 OpenCV CUDA 加速）
2. 按设定间隔抽取关键帧
3. 自动保存至 `项目目录/images/` 文件夹
4. 生成缩略图网格视图

**输出**
- 图像序列（JPG 格式，保留原始分辨率）
- 图像管理界面（缩略图浏览）

**交互设计**
```
┌─────────────────────────────────────────────────────┐
│  导入视频：[选择文件] 或 拖拽视频到此处              │
│  抽帧间隔：[5] 帧  (推荐: 5-10帧)                    │
│                  [开始抽帧]                          │
├─────────────────────────────────────────────────────┤
│  已导入图片 (120张)           [批量删除] [全部清空]  │
│  ┌───┬───┬───┬───┬───┐                              │
│  │ ✓ │ ✓ │ X │ ✓ │ ✓ │  (点击切换选中状态)          │
│  └───┴───┴───┴───┴───┘                              │
│  提示：建议每个场景准备200张以上图片，2个以上视角     │
└─────────────────────────────────────────────────────┘
```

**优化建议**
- 支持批量导入多个视频文件
- 添加自动去模糊检测（过滤质量差的帧）
- 提供场景分类功能（按视角/日期自动分组）

---

#### 🎯 模块二：标注图片

**功能目标**：半自动化标注工作流，提升标注效率

**预定义关键点集合**

| 类别 | 关键点数量 | 关键点定义 |
|------|-----------|-----------|
| **人体关键点**<br>(Halpe26) | 26个 | Nose, LEye, REye, LEar, REar, LShoulder, RShoulder, LElbow, RElbow, LWrist, RWrist, LHip, RHip, LKnee, RKnee, LAnkle, RAnkle, Head, Neck, Hip, LBigToe, RBigToe, LSmallToe, RSmallToe, LHeel, RHeel |
| **扎奇奥尔斯基<br>生物力学模型** | 23个 | 颅顶点, 第七颈椎, 胸骨下点, 脐点, 耻骨点, 左肩峰, 左肘, 左腕, 左中指尖, 左髋, 左膝, 左踝, 左足跟, 左趾尖, 右肩峰, 右肘, 右腕, 右中指尖, 右髋, 右膝, 右踝, 右足跟, 右趾尖 |
| **弓箭器械关键点**<br>(自定义) | 5个 | 上弓梢点(UP), 下弓梢点(DOWN), 箭羽前缘点(FL), 主防震杆远端(ST), 护弓绳点(FS) |

**处理流程**
1. **自动预标注**（使用 rtmpose-x-halpe26.onnx）
   - 自动检测人体26个关键点
   - 生成初始置信度分数
   - 标注结果实时可视化

2. **手动标注自定义关键点**
   - 左键点击标注弓箭5个关键点
   - 支持快捷键切换关键点类型（1-5）
   - 鼠标悬停显示关键点名称

3. **自动生成边界框**
   - 点击"生成边界框"按钮
   - 根据关键点坐标自动计算外接矩形
   - 生成两类边界框：
     - 人体边界框（基于 Halpe26 点）
     - 弓箭边界框（基于自定义5点）

4. **手动调整**
   - 拖拽调整关键点位置
   - 拉伸调整边界框大小
   - 重命名关键点（针对特殊场景）

**交互设计**
```
┌─────────────────────┬───────────────────────────────────┐
│ 图像列表 (120张)     │  标注工作区                        │
│ ☑ img_001.jpg       │  ┌─────────────────────────────┐  │
│ ☐ img_002.jpg       │  │                             │  │
│ ☐ img_003.jpg       │  │    [图像显示区域]            │  │
│ ...                 │  │    • 关键点显示              │  │
│                     │  │    • 边界框显示              │  │
│ [上一张] [下一张]    │  │    • 骨架连线显示            │  │
│                     │  └─────────────────────────────┘  │
│ 标注模式：           │                                   │
│ ○ 人体关键点(自动)   │  工具栏：                          │
│ ● 弓箭关键点(手动)   │  [自动预测] [生成边界框] [保存]   │
│                     │                                   │
│ 快捷键：             │  当前关键点: 上弓梢点 (UP)         │
│ 1-5: 切换关键点     │  坐标: (512, 384) 置信度: 0.95    │
│ Delete: 删除关键点   │                                   │
│ Ctrl+S: 保存        │  进度: 45/120 已标注 (37.5%)      │
└─────────────────────┴───────────────────────────────────┘
```

**数据格式（COCO JSON）**
```json
{
  "images": [...],
  "annotations": [
    {
      "id": 1,
      "image_id": 1,
      "category_id": 1,
      "bbox": [x, y, width, height],
      "keypoints": [x1,y1,v1, x2,y2,v2, ...],
      "num_keypoints": 31
    }
  ],
  "categories": [
    {
      "id": 1,
      "name": "archery_person",
      "keypoints": ["Nose", "LEye", ..., "UP", "DOWN", "FL", "ST", "FS"],
      "skeleton": [[0,1], [1,3], ...]
    }
  ]
}
```

**优化建议**
- 添加"智能插值"功能：根据前后帧自动推测当前帧关键点位置
- 提供标注质量评估：检测异常关键点（如位置突变）
- 支持标注模板保存：针对固定机位快速复用关键点配置

---

#### 🧠 模块三：训练模型

**功能目标**：一键式模型训练，自动调参和评估

**训练配置**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 基础模型 | RTMO-S | 轻量级版本，适合实时推理 |
| 训练策略 | rtmo-s_8xb32-600e | 600 epochs，batch size 32 |
| 输入分辨率 | 640×640 | 平衡精度和速度 |
| 学习率 | 0.004 | 使用 Cosine Annealing |
| 数据增强 | ✓ | 随机翻转、旋转、缩放、颜色抖动 |
| 验证集比例 | 20% | 自动从标注数据中划分 |

**处理流程**
1. **数据集准备**
   - 自动划分训练集/验证集（80%/20%）
   - 生成 MMPose 格式配置文件
   - 数据增强预处理

2. **模型微调**
   - 加载预训练权重（COCO pretrained）
   - 冻结 Backbone，仅训练 Head
   - 动态学习率调整

3. **训练监控**
   - 实时显示 Loss 曲线
   - 验证集 mAP（Mean Average Precision）
   - 预计剩余时间

4. **模型导出**
   - 自动保存最佳权重（best.pth）
   - 导出 ONNX 格式（用于推理）
   - 生成训练报告（PDF）

**交互设计**
```
┌─────────────────────────────────────────────────────┐
│  训练配置                                            │
│  数据集: 120张图片 (96训练, 24验证)                  │
│  基础模型: [RTMO-S ▼]  (可选: RTMO-M, RTMO-L)       │
│  训练轮数: [600] epochs                              │
│  批次大小: [32]                                      │
│                                                      │
│             [开始训练]  [停止训练]                   │
├─────────────────────────────────────────────────────┤
│  训练进度: Epoch 45/600  ████░░░░░░  7.5%            │
│  剩余时间: 约 2小时15分钟                            │
│                                                      │
│  Loss 曲线:                                          │
│  1.5 │\                                              │
│  1.0 │ \___                                          │
│  0.5 │     \____                                     │
│  0.0 └──────────────────────────────                │
│       0   100   200   300   400   500                │
│                                                      │
│  验证集 mAP: 0.87  (最佳: 0.89 @ Epoch 42)           │
│                                                      │
│  GPU 使用率: 85%  显存: 6.2GB / 8GB                  │
└─────────────────────────────────────────────────────┘
```

**优化建议**
- 支持断点续训：意外中断后可恢复训练
- 添加"迭代优化"模式：训练完成后用新模型重新标注，形成飞轮
- 提供预设配置：根据数据集规模自动推荐超参数

---

#### 🎥 模块四：应用模型

**功能目标**：实时关键点检测和坐标追踪

**输入参数**

| 参数 | 类型 | 说明 |
|------|------|------|
| 受试者身高 | 数值(cm) | 用于3D尺度恢复 |
| 受试者性别 | 选项 | 影响生物力学参数 |
| 相机标定文件 | .yaml | OpenCV 标定参数（内参+外参） |
| 摄像头ID | 整数 | 默认0（可多摄像头） |

**处理流程**
1. **相机初始化**
   - 加载相机标定参数
   - 计算畸变矫正矩阵
   - 建立世界坐标系

2. **实时推理**
   - 读取摄像头画面（30 FPS）
   - ONNX Runtime GPU 推理
   - 关键点坐标后处理

3. **坐标平滑**
   - 可选 EMA（指数移动平均）滤波
   - 频率可调（5-30 Hz）
   - 减少抖动和噪声

4. **可视化显示**
   - 实时绘制关键点和骨架
   - 叠加坐标数值
   - 显示置信度热力图

**交互设计**
```
┌─────────────────────────────────────────────────────┐
│  受试者信息                                          │
│  身高: [175] cm  性别: ○男 ○女                       │
│  标定文件: [camera_calibration.yaml] [选择文件]     │
│                                                      │
│  摄像头设置                                          │
│  主摄像头: [摄像头0 ▼]  分辨率: [1920×1080 ▼]       │
│  辅摄像头: [摄像头1 ▼]  (可选，用于双视角)           │
│                                                      │
│  滤波设置                                            │
│  ☑ 启用EMA滤波  平滑系数: [0.7] (0-1)               │
│                                                      │
│             [开始检测]  [停止检测]                   │
├─────────────────────────────────────────────────────┤
│  实时画面 (主摄像头)                                 │
│  ┌───────────────────────────────────────┐          │
│  │                                       │          │
│  │    [实时视频显示]                      │          │
│  │    • 关键点叠加                        │          │
│  │    • 骨架连线                          │          │
│  │    • 坐标数值标注                      │          │
│  │                                       │          │
│  └───────────────────────────────────────┘          │
│  FPS: 32  推理时间: 28ms  总人数: 1                  │
│                                                      │
│  关键坐标实时显示：                                  │
│  上弓梢点(UP): (512, 384, 102) mm  置信度: 0.94     │
│  下弓梢点(DOWN): (518, 620, 98) mm  置信度: 0.91    │
│  ...                                                │
└─────────────────────────────────────────────────────┘
```

**优化建议**
- 添加"录制模式"：保存实时检测视频和坐标数据
- 支持多人同时检测（训练场景）
- 提供姿态评分系统：基于标准模板对比当前姿态

---

#### 📊 模块五：导出数据

**功能目标**：多格式数据导出，满足科研分析需求

**导出选项**

| 格式 | 内容 | 适用场景 |
|------|------|---------|
| 原始坐标 CSV | 2D像素坐标 + 时间戳 | 基础数据分析 |
| 滤波后 CSV | 巴特沃斯低通滤波(10Hz) | 运动学分析 |
| 3D坐标 CSV | 世界坐标系(mm) + 时间戳 | 生物力学研究 |
| 可视化视频 | 关键点叠加视频(MP4) | 报告展示 |
| 分析报告 | PDF报告(含图表) | 专业汇报 |

**滤波参数**
- **四阶双向巴特沃斯低通滤波器**
  - 截止频率：10 Hz（符合人体运动频谱）
  - 采样频率：自动检测（通常30-60 Hz）
  - 零相位失真（前向+后向滤波）

**3D重建算法**
- 基于双目立体视觉
- DLT（直接线性变换）方法
- 畸变校正 + 三角测量

**交互设计**
```
┌─────────────────────────────────────────────────────┐
│  选择导出内容                                        │
│  ☑ 原始2D坐标 (CSV)                                  │
│  ☑ 滤波后坐标 (CSV, 10Hz 巴特沃斯)                   │
│  ☑ 3D世界坐标 (CSV, 需双摄像头标定)                  │
│  ☐ 可视化视频 (MP4, 1920×1080, 30fps)               │
│  ☐ 分析报告 (PDF, 包含运动轨迹图)                    │
│                                                      │
│  导出设置                                            │
│  时间范围: [0:00] - [5:32]  (全部/自定义)            │
│  坐标系统: ○像素坐标 ●世界坐标(mm)                   │
│  输出目录: [D:\射箭数据\2026-02-13\] [浏览]          │
│                                                      │
│             [开始导出]  [取消]                       │
├─────────────────────────────────────────────────────┤
│  导出进度: ████████████████░░░░ 80%                  │
│  正在处理: 生成3D坐标数据...                         │
│                                                      │
│  已完成:                                             │
│  ✓ raw_keypoints_2d.csv (125KB)                     │
│  ✓ filtered_keypoints_2d.csv (118KB)                │
│  ⏳ keypoints_3d.csv (处理中...)                     │
└─────────────────────────────────────────────────────┘
```

**CSV 数据格式示例**
```csv
timestamp,frame,nose_x,nose_y,nose_conf,leye_x,leye_y,leye_conf,...,up_x,up_y,up_conf
0.000,0,512.3,384.7,0.95,498.2,372.1,0.93,...,510.8,102.4,0.91
0.033,1,512.5,384.9,0.94,498.4,372.3,0.92,...,511.2,102.6,0.90
...
```

**优化建议**
- 支持导出为 C3D 格式（运动分析软件通用格式）
- 添加自动报告生成：包含速度、加速度、角度等派生指标
- 提供数据对比功能：叠加多次训练数据进行纵向分析

---

## 4. 数据流设计

### 4.1 完整数据链路
```
视频文件 (MP4)
    ↓ 【模块1】抽帧
图像序列 (JPG)
    ↓ 【模块2】标注
COCO JSON 数据集
    ↓ 【模块3】训练
ONNX 模型文件
    ↓ 【模块4】推理
实时坐标流 (NumPy数组)
    ↓ 【模块5】后处理
CSV / 可视化报告
```

### 4.2 项目文件结构
```
项目名称/
├── images/              # 原始图片
│   ├── cam1/            # 摄像头1
│   └── cam2/            # 摄像头2
├── annotations/         # 标注文件
│   ├── train.json
│   └── val.json
├── models/              # 训练产物
│   ├── best.pth
│   ├── best.onnx
│   └── training_log.txt
├── calibration/# 相机标定
│   ├── camera1.yaml
│   ├── camera2.yaml
│   └── stereo.yaml
└── output/              # 导出数据
    ├── raw_2d.csv
    ├── filtered_2d.csv
    └── 3d_coords.csv
```

---

## 5. UI/UX 设计规范

### 5.1 整体布局
```
┌───────────────────────────────────────────────────────┐
│  射箭Plus  v1.0           最小化 □ 最大化 ✕             │
├────────┬──────────────────────────────────────────────┤
│        │  标题栏: 当前模块名称                          │
│        ├──────────────────────────────────────────────┤
│ 工作流 │                                               │
│ 按钮栏 │           主工作区                             │
│ (左)   │           (动态切换内容)                       │
│        │                                               │
│ ① 导入 │                                               │
│ ② 标注 │                                               │
│ ③ 训练 │                                               │
│ ④ 应用 │                                               │
│ ⑤ 导出 │                                               │
│        │                                               │
├────────┼──────────────────────────────────────────────┤
│ 状态栏 │  GPU: RTX3060 (85%) | 内存: 8.2GB | 就绪      │
└────────┴──────────────────────────────────────────────┘
```

### 5.2 设计原则
1. **渐进式披露**：初学者看到简化界面，高级用户可展开参数
2. **实时反馈**：所有操作提供进度提示和预计时间
3. **错误友好**：错误信息提供解决方案，而非技术术语
4. **快捷键支持**：提升专业用户效率

### 5.3 色彩体系
- 主色调：深蓝 (#1E3A8A) - 科技感、专业
- 辅助色：青绿 (#10B981) - 激活状态
- 警告色：橙色 (#F59E0B) - 提示
- 错误色：红色 (#EF4444) - 错误

---

## 6. 关键技术实现

### 6.1 CUDA 加速优化
```python
# OpenCV CUDA 加速示例
import cv2

# 启用 CUDA 后端
cv2.cuda.setDevice(0)
gpu_frame = cv2.cuda_GpuMat()
gpu_frame.upload(frame)

# GPU 上进行预处理
gpu_resized = cv2.cuda.resize(gpu_frame, (640, 640))
processed = gpu_resized.download()
```

### 6.2 ONNX Runtime GPU 推理
```python
import onnxruntime as ort

# 使用 CUDA 执行提供器
providers = ['CUDAExecutionProvider', 'CPUExecutionProvider']
session = ort.InferenceSession('model.onnx', providers=providers)

# 推理
outputs = session.run(None, {'input': input_tensor})
```

### 6.3 EMA 滤波实现
```python
class EMAFilter:
    def __init__(self, alpha=0.7):
        self.alpha = alpha
        self.prev = None
    
    def update(self, value):
        if self.prev is None:
            self.prev = value
        else:
            self.prev = self.alpha * value + (1 - self.alpha) * self.prev
        return self.prev
```

### 6.4 巴特沃斯滤波
```python
from scipy.signal import butter, filtfilt

def butterworth_filter(data, cutoff=10, fs=30, order=4):
    nyquist = 0.5 * fs
    normal_cutoff = cutoff / nyquist
    b, a = butter(order, normal_cutoff, btype='low', analog=False)
    return filtfilt(b, a, data, axis=0)  # 零相位滤波
```

---

## 7. 项目可行性分析

### 7.1 技术可行性 ⭐⭐⭐⭐⭐
- ✅ MMPose 生态成熟，文档完善
- ✅ ONNX Runtime GPU 性能优异
- ✅ PyQt6 跨平台稳定
- ⚠️ 3D重建需要专业标定（需提供标定工具或教程）

### 7.2 开发成本估算

| 阶段 | 工作量 | 关键任务 |
|------|--------|---------|
| 原型开发 | 2-3周 | 核心功能验证（模块2+3） |
| UI 开发 | 3-4周 | PyQt6 界面实现 |
| 集成测试 | 2周 | 端到端流程测试 |
| 文档编写 | 1周 | 用户手册、API文档 |
| **总计** | **8-10周** | 单人开发 |

### 7.3 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| GPU 兼容性问题 | 高 | 提供 CPU fallback 模式 |
| 标注效率低 | 中 | 迭代优化自动标注精度 |
| 3D重建误差大 | 中 | 提供标定质量检测工具 |
| 用户学习曲线陡峭 | 低 | 制作视频教程 + 内置引导 |

---

## 8. 开发路线图

### 8.1 MVP（最小可行产品）v0.5
**目标**：验证核心流程，2周完成
- ✅ 模块2：标注图片（手动标注）
- ✅ 模块3：训练模型（命令行版）
- ✅ 模块4：应用模型（简单GUI）
- ❌ 3D功能（暂不实现）

### 8.2 Beta 版本 v1.0
**目标**：完整工作流，6周完成
- ✅ 所有5个模块
- ✅ 2D 实时检测
- ✅ 数据导出（CSV）
- ⚠️ 3D重建（实验性功能）

### 8.3 正式版本 v1.5
**目标**：优化用户体验，4周完成
- ✅ 完善的 UI/UX
- ✅ 详细用户手册
- ✅ 自动更新机制
- ✅ 性能优化（帧率提升）

---

## 9. 推荐优化方案

### 9.1 架构优化
1. **插件化设计**：将关键点定义做成插件，支持用户自定义运动项目（如网球、高尔夫）
2. **云端协作**：支持标注数据云端同步，团队协作标注
3. **模型市场**：内置模型商店，下载预训练模型（如标准射箭姿态）

### 9.2 功能扩展
1. **姿态评分系统**
   - 定义标准射箭姿态模板
   - 实时对比并打分（0-100分）
   - 生成改进建议

2. **运动轨迹分析**
   - 箭羽运动轨迹追踪
   - 弓弦释放速度计算
   - 关节角度变化曲线

3. **数据库管理**
   - SQLite 存储历史训练数据
   - 运动员档案管理
   - 长期进步追踪

### 9.3 用户体验优化
1. **智能引导**：首次使用时提供交互式教程
2. **模板预设**：内置"射箭标准模式"、"康复训练模式"等
3. **一键分享**：导出结果支持直接分享到社交平台

---

## 10. 技术支持计划

### 10.1 用户文档
- 📖 快速入门指南（PDF + 视频）
- 📖 高级功能手册
- 📖 常见问题 FAQ
- 📖 相机标定教程

### 10.2 开发者文档
- 🛠️ API 参考文档
- 🛠️ 插件开发指南
- 🛠️ 自定义模型训练指南

### 10.3 社区支持
- 💬 官方论坛 / Discord 频道
- 📧 技术支持邮箱
- 🐛 GitHub Issues 跟踪

---

## 11. 商业化建议

### 11.1 定价策略
- **个人版**：免费（功能限制：最多200张标注图片）
- **专业版**：¥1,999/年（无限制 + 优先支持）
- **企业版**：¥9,999/年（多用户 + 定制开发）

### 11.2 盈利模式
1. 软件许可证销售
2. 定制化模型训练服务
3. 数据分析咨询服务
4. 教育培训课程

---

## 附录

### A. 参考资料
- [MMPose 官方文档](https://mmpose.readthedocs.io/)
- [RTMO 论文](https://arxiv.org/abs/2312.07526)
- [ONNX Runtime 性能优化](https://onnxruntime.ai/docs/performance/)

### B. 术语表
- **关键点（Keypoint）**：人体或物体上的特定标志性位置
- **边界框（Bounding Box）**：矩形框，用于框定检测对象
- **mAP**：平均精度均值，评估检测模型性能的指标
- **EMA**：指数移动平均，一种平滑滤波算法

---

**文档版本**：v1.0  
**创建日期**：2026-02-13  
**最后更新**：2026-02-13  
**负责人**：射箭Plus 项目组