# 射箭Plus 实施计划（实时同步版）

更新时间：2026-03-01  
项目状态：`2D MVP` 已完成“导入-标注-训练-应用-导出”首轮闭环，当前进入“工程治理补强 + 训练稳定化 + 导出完善”阶段

## 1. 项目摘要

射箭Plus 当前已经不是单纯的界面原型，而是具备真实业务闭环的桌面科研工具：

1. 可导入素材并初始化项目结构。
2. 可进行人体关键点、弓箭关键点、矩形框标注。
3. 可接入 ONNX 自动预标注。
4. 可基于 MMPose 生成训练数据与训练配置并启动训练。
5. 可用单模型或双模型对视频进行实时推理。
6. 可导出标注结果与推理时序结果。

当前最需要解决的问题，已经从“能不能做出来”，转向“能不能稳定维护、稳定复现、稳定协作”。

## 2. 当前已完成能力

### 2.1 工程与主界面

1. `run.py` + `src/shejianplus` 工程骨架可运行。
2. 主窗口 5 模块框架稳定：导入、标注、训练、应用、导出。
3. 当前源码按 `src layout` 组织，具备继续扩展的基础。

### 2.2 模块1：导入素材

1. 支持项目目录初始化。
2. 支持图片列表加载与基础日志反馈。
3. 项目目录初始化逻辑已可复用。

### 2.3 模块2：标注图片

1. 标注页已改为标签驱动，支持导入标签 JSON、添加关键点标签、添加矩形标签。
2. 支持人体关键点、弓箭关键点、bbox 双类型标注。
3. 点与框支持新增、拖拽、右键删除。
4. 自动保存开关可用。
5. 左下角显示鼠标像素坐标与图片序号。
6. 自动预标注参数统一为 `confidence + IoU`。
7. 人体自动预标注已按 `id` 对齐，修复了关键点名称错位问题。

### 2.4 模块3：训练模型

1. 训练页已形成左右布局，支持参数配置、日志显示、状态显示与曲线绘制。
2. 训练配置已支持单目标训练流程。
3. `input_size` 已支持可编辑宽高，HALPE26 默认值自动填充。
4. 已支持构建 MMPose 数据集、生成训练配置、填充命令并启动训练。
5. 已修复 evaluator 类型冲突、metainfo 合并冲突、Windows 子进程环境兼容等问题。
6. `RTMO-s` 预训练权重支持自动填充。

### 2.5 模块4：应用模型

1. 当前支持视频文件实时推理。
2. 支持三种推理目标：
- 弓箭关键点（RTMO）
- 人体关键点（RTMPose-HALPE26）
- 双模型同时推理
3. 支持实时滤波：
- One Euro（一欧元滤波器）
- 双向四阶巴特沃斯近实时方案
4. 支持弓长像素到厘米换算。
5. 支持实时 FPS、视频 FPS、丢帧状态显示。
6. 支持参数分析：
- 瞬时速度
- 轨迹回看
- 两点 / 三点 / 四点角度计算

### 2.6 模块5：导出数据

1. 导出页已具备实际操作能力，不再是占位页。
2. 支持导出标注结果：
- `raw_2d.csv`
- `filtered_2d.csv`
- `bbox_2d.csv`
- `annotations_export.json`
- `label_schema_export.json`
3. 支持在导出页统一导出推理时序结果：
- `raw_2d_timeseries.csv`
- `filtered_2d_timeseries.csv`
- `session_meta.json`

## 3. 当前核心问题诊断

### 3.1 工程治理问题

1. 根目录资源仍然偏杂：
- 模型文件、配置文件、示例视频目前仍位于仓库根目录。
- 这不影响运行，但会持续降低项目可读性。

2. 样例数据仍在 Git 中：
- `workspace/demo_project/...` 已被历史提交跟踪。
- `56val12times-2.avi` 仍在 Git 跟踪中。
- 这比“根目录视觉杂乱”更值得优先处理，因为它会持续污染仓库历史。

3. 虚拟环境目录位于仓库根目录：
- 历史上使用 `shejianplus/` 作为虚拟环境目录，视觉上容易和项目代码混淆。
- 本轮开始统一虚拟环境目录约定为 `.venv/`。

### 3.2 依赖与环境问题

1. `requirements.txt` 目前只覆盖 GUI 与推理基础依赖。
2. 训练依赖仍然依赖 README 中的手工命令安装。
3. 当前项目尚不具备“一条命令完整复现环境”的能力。

### 3.3 测试与质量保障问题

1. 当前没有 `tests/` 目录。
2. 核心模块没有自动化回归测试：
- 标签/标注数据结构
- 滤波算法
- 尺度换算
- 导出结果
- 训练配置生成
3. 当前改动主要依赖手工验证，双人协作后风险会逐渐放大。

### 3.4 自动化问题

1. 当前没有 GitHub Actions。
2. 没有自动语法检查。
3. 没有自动运行最小测试。
4. 没有在 PR 层面形成基础门禁。

### 3.5 产品层面问题

1. 训练闭环虽然已跑通，但训练结果管理仍偏弱：
- best/latest 权重入口不清晰
- 训练完成后的样例验证能力不足

2. 应用模块已可用，但还缺少更完整的交付能力：
- 摄像头输入
- 叠加视频导出
- 更直观的双模型结果视图

## 4. 里程碑状态

### 里程碑A：导入 + 标注闭环

状态：`已完成（增强版）`

验收结论：
1. 可导入项目与图片列表。
2. 可进行关键点与矩形双类型标注。
3. 支持标签导入、标签新增、自动保存。
4. 支持自动预标注与批量自动预标注。
5. 已完成关键点错位映射修正。

### 里程碑B：训练闭环

状态：`进行中（v0.3，已可稳定启动训练）`

已达成：
1. GUI 参数化配置训练任务。
2. 构建 MMPose 数据集（train/val/test）。
3. 生成训练配置并填充训练命令。
4. 启动训练子进程并显示日志。
5. 显示实时曲线与训练状态指标。

待补强：
1. 指标解析鲁棒性继续优化。
2. 训练产物管理与可视化验证入口。
3. 训练环境复现体验。

### 里程碑C：应用 + 导出

状态：`进行中（模块4首版可用 + 模块5 v0.2）`

已达成：
1. 视频实时推理可用。
2. 双模型推理可用。
3. 推理时序导出可用。
4. 标注导出可用。

待补强：
1. 摄像头输入。
2. 叠加视频导出。
3. ONNX 导出与回归验证入口。

### 里程碑D：工程治理与团队协作

状态：`未完成，但已开始建设`

目标：
1. 清理仓库中的样例数据与临时资源。
2. 补齐最小测试骨架。
3. 接入 GitHub Actions 最小 CI。
4. 明确依赖锁定与目录治理策略。
5. 形成双人协作可持续的工程基础。

## 5. 接下来 2 到 4 周详细计划

### 5.1 P0：仓库治理与资源整理

目标：让仓库从“能用”进入“可维护”。

计划：
1. 清理 Git 中已跟踪的 `workspace/demo_project` 样例数据。
2. 清理 Git 中已跟踪的 `56val12times-2.avi`。
3. 根目录资源分层：
- 新增 `models/` 目录存放本地模型
- 新增 `assets/` 目录存放示例视频或演示资源
- 仅保留源码、配置、文档在根目录
4. 在文档中明确：
- 模型文件不进 Git
- 样例视频不进 Git
- 运行生成的数据不进 Git

验收标准：
1. `git ls-files workspace` 无样例数据。
2. `git ls-files *.avi` 无示例视频。
3. 仓库结构更清晰，协作者一眼可分辨源码与资源。

### 5.2 P0：测试体系首版

目标：建立最小化自动回归能力。

第一批测试建议覆盖：
1. `keypoint_schema`
- 标签集合 JSON 校验
- `id` 连续性校验
- 名称唯一性校验

2. `ema_filter`
- One Euro 基本更新逻辑
- 参数变化不会导致异常

3. `scale_converter`
- UP/DOWN 正常时比例计算
- 低置信度时冻结比例
- 距离过小时保护逻辑

4. `inference_session_exporter` / `exporter`
- 导出字段完整性
- 空数据时的容错行为

验收标准：
1. 仓库出现 `tests/` 目录。
2. 至少有 4 组基础测试可运行。
3. 改动核心逻辑后，可以用测试快速判断是否回归。

### 5.3 P0：GitHub Actions 最小 CI

目标：把最小质量门禁自动化。

首版 CI 只做轻量检查：
1. 安装基础依赖。
2. 运行 `python -m py_compile`。
3. 运行最小单元测试。

说明：
1. 首版不在 CI 中跑训练。
2. 首版不要求下载大模型。
3. 首版不做 GUI 端到端测试。

验收标准：
1. `.github/workflows/` 下出现最小 CI。
2. Pull Request 自动触发检查。
3. 失败时能明确指出是语法错误还是测试错误。

### 5.4 P1：依赖管理重构

目标：降低环境复现难度。

建议方向：
1. 将基础依赖和训练依赖拆分：
- `requirements.txt`：GUI / 推理基础依赖
- `requirements-train.txt`：MMPose 训练依赖

2. 后续可选升级方案：
- `pip-tools` 锁定版本
- 或迁移到 `pyproject.toml`

阶段目标：
1. 让协作者至少能根据文档稳定复现环境。
2. 让训练环境和 GUI 环境边界更清楚。

### 5.5 P1：训练模块补强

目标：从“能启动训练”走向“能稳定迭代模型”。

计划：
1. 优化日志解析鲁棒性。
2. 增加训练结果文件快捷入口：
- 配置文件
- 日志文件
- best 权重
- latest 权重
3. 增加训练完成后的样例验证入口。
4. 补充训练参数说明文档。

### 5.6 P1：应用与导出补强

目标：从“可演示”走向“可交付”。

计划：
1. 应用模块接入摄像头输入。
2. 增加叠加视频导出。
3. 优化双模型显示视图。
4. 增加推理会话记录与导出配置记录。

## 6. 模块级详细下一步计划

### 6.1 标注模块

1. 固化标签 JSON 标准。
2. 增加导入失败时更明确的错误提示。
3. 检查 bbox 与关键点混合编辑时的交互一致性。
4. 为标注存储结构补测试。

### 6.2 训练模块

1. 明确训练结果目录结构。
2. 增加 best/latest 权重可视入口。
3. 增加训练完成后的样例回放或推理验证。
4. 提升不同模型配置兼容性。

### 6.3 应用模块

1. 接入摄像头输入。
2. 补充图例、状态说明、错误提示。
3. 优化高帧率视频下的丢帧与显示策略。
4. 继续打磨参数分析面板。

### 6.4 导出模块

1. 增加叠加视频导出。
2. 增加导出配置快照。
3. 增加导出结果预览或结果摘要。
4. 为导出器逻辑补测试。

## 7. 工程治理计划

### 7.1 目录治理目标

建议未来整理成如下方向：

1. 根目录只保留：
- 源码
- 配置
- 文档
- 启动入口

2. 资源目录拆分：
- `models/`：本地模型文件
- `assets/`：演示视频、示例资源
- `workspace/`：运行时项目数据，不进入 Git

3. 环境目录统一：
- 优先考虑 `.venv/`
- 虚拟环境目录不作为项目结构的一部分理解

### 7.2 文档治理目标

1. `README.md`：面向使用者，强调安装、运行、训练、导出。
2. `实施计划.md`：面向项目推进，记录现状、问题、优先级与路线图。
3. `CONTRIBUTING.md`：面向协作者，约定分支、提交、评审、自测。

## 8. 测试与 CI 建设计划

### 8.1 第一阶段

1. 建立 `tests/` 骨架。
2. 接入 `pytest`。
3. 覆盖最核心的纯逻辑模块。

### 8.2 第二阶段

1. 增加训练配置生成相关测试。
2. 增加导出器测试。
3. 增加数据结构兼容性测试。

### 8.3 第三阶段

1. 视情况加入更轻量的 UI 逻辑测试。
2. 增加关键流程的集成测试脚本。

## 9. 风险与注意事项

1. 训练环境仍依赖 `mmpose==1.3.2`，环境兼容是持续风险。
2. 不同模型的配置继承结构不同，训练配置生成需要继续保持动态兼容。
3. 历史标注数据不做破坏性迁移，不匹配项仍应以告警和兼容为主。
4. 高速视频推理与实时滤波之间存在性能与显示延迟的平衡问题。
5. 当前最容易被忽视的技术债不是功能缺失，而是“无测试、无 CI、样例数据入库”。

## 10. 近期执行顺序

建议后续开发按下面顺序推进：

1. 清理 Git 中的 `workspace` 和示例视频。
2. 建立第一批单元测试。
3. 接入 GitHub Actions 最小 CI。
4. 整理依赖结构与环境文档。
5. 继续补强训练结果管理。
6. 继续补强应用与导出模块。

## 11. 本周目标

1. 工程治理进入可执行阶段
- 样例数据清理方案明确
- 协作文档已补齐
- 下一步转向测试与 CI

2. 训练模块达到“可观测 + 可复现”的更高标准
- 配置稳定生成
- 日志与结果可追踪
- 环境复现路径更明确

3. 应用与导出模块继续向可交付形态推进
- 双模型推理稳定化
- 推理时序导出完善
- 为叠加视频导出做准备
