# 射箭Plus 实施计划（进度对齐版）

更新时间：2026-02-13  
项目状态：`2D MVP` 已进入“可演示 + 可持续迭代”阶段

## 1. 当前进展总览

## 已完成

1. 工程骨架与运行入口
- 已完成 `run.py` + `src/archery_plus` 包结构。
- 已完成主窗口、5模块页面基础框架。

2. 模块1（导入素材）
- 已接入 OpenCV 抽帧：支持 MP4/AVI、抽帧间隔 1~30。
- 已接入项目目录初始化：`images/cam1` 等目录自动创建。
- 已完成日志与结果反馈（总帧数、保存帧数）。

3. 模块2（标注图片）MVP
- 已完成图片列表加载（`images/cam1`）。
- 已完成标注画布：点选、拖拽、删除。
- 已完成关键点类别切换（UP/DOWN/FL/ST/FS）。
- 已完成标注保存与回读（`annotations/manual_annotations.json`）。
- 已完成“新增点后自动保存并切到下一张”。

4. UI 与可用性
- 左侧导航/标注页布局可用并持续调优。
- 关键状态信息显示（模型资产状态、点数、底部运行状态栏）。

## 进行中

1. 标注显示一致性微调（全屏场景视觉一致性）
- 逻辑已支持 16:9 显示区 + 原图等比显示。
- 继续做视觉层面的最终校准与固定。

## 未完成

1. 模块2 自动预标注（Halpe26 ONNX 推理）尚未接入。  
2. 模块3 训练调度与日志监控尚未接入。  
3. 模块4 实时摄像头推理尚未接入。  
4. 模块5 CSV 导出（raw/filtered）尚未接入。  
5. COCO 31点标准输出尚未替换当前 JSON 存储。

---

## 2. 里程碑状态

## 里程碑A：导入 + 标注最小闭环
状态：`已完成（MVP）`

验收结论：
1. 已可从视频抽帧到图片目录。
2. 已可逐图标注、保存、回读。
3. 已支持连续标注操作（自动跳下一张）。

## 里程碑B：训练闭环
状态：`未开始`

目标：
1. 从标注数据自动拆分 train/val。
2. 通过 GUI 启动训练任务并显示日志。
3. 产出可推理模型文件（ONNX）。

## 里程碑C：应用 + 导出
状态：`未开始`

目标：
1. 摄像头实时推理显示关键点与 FPS。
2. 导出 raw_2d.csv / filtered_2d.csv。

---

## 3. 本周建议任务（按优先级）

1. 接入 Halpe26 ONNX 自动预标注（模块2增强）
- 输入：当前图片
- 输出：人体关键点初始点位
- 在画布上可编辑后保存

2. 升级标注存储到 COCO 结构
- 将 `manual_annotations.json` 迁移为 COCO 兼容数据结构
- 先支持单类别 + 31关键点

3. 训练入口最小版（模块3）
- 支持“启动训练脚本 + 日志滚动显示”
- 暂不做高级训练面板，先跑通命令

4. 实时推理占位改真实输入（模块4）
- 先单摄像头 + 单模型推理
- 先实现图像帧循环与显示，再接关键点绘制

---

## 4. 技术债与注意事项

1. 乱码与编码一致性
- 所有 `.py/.md` 强制使用 UTF-8（无 BOM）。

2. UI 调整频繁
- 建议把关键尺寸集中到常量，减少重复手改。

3. 标注数据格式演进
- 当前 JSON 可用，但应尽快切 COCO，避免后续训练对接反复改造。

---

## 5. 下一阶段目标（两周）

1. 达到“可训练”里程碑
- 从图片标注 -> 训练命令 -> 模型产出

2. 达到“可推理”里程碑
- 摄像头实时显示关键点 + 基础 CSV 导出

3. 保持策略
- 优先打通端到端流程，再做性能和界面精修。
