# 射箭Plus 实施计划（实时同步版）

更新时间：2026-02-17  
项目状态：`2D MVP` 已进入“训练闭环稳定化 + 指标可视化增强”阶段

## 1. 当前进展总览

## 已完成

1. 工程与主界面
- `run.py` + `src/archery_plus` 工程骨架可运行。
- 主窗口 5 模块框架稳定：导入、标注、训练、应用、导出。

2. 模块1（导入素材）
- 项目目录初始化、抽帧入口、日志反馈可用。
- 项目目录结构初始化逻辑可复用。

3. 模块2（标注图片）重构完成（本轮重点）
- 标注页已改为“标签驱动”，移除“模型资产状态”区块。
- 新增“标签”模块，支持：
  - 导入标签 JSON
  - 添加关键点标签
  - 添加矩形标签（目标检测标签）
- 标注模式支持：
  - 关键点标注（人体/弓箭）
  - 矩形框标注（bbox）
- 画布支持：
  - 点的新增/拖拽/右键删除
  - 框的新增/拖拽/右键删除
- 取消“显示人体点”按钮，统一显示标注。
- 自动保存开关继续可用：新增点、删点、新增框、删框都可自动保存。
- 左下角新增鼠标像素坐标与图片序号显示：
  - `(X:,Y:)[图片名称:当前/总数]`
- 自动预标注参数 UI：
  - 保留置信度
  - 去掉 NMS 文案，改为 IoU 控件

4. 标注数据结构
- 每图结构已支持：
  - `human_keypoints`
  - `archery_keypoints`
  - `bboxes`
- 标签 schema 文件已引入：
  - `<project>/annotations/label_schema.json`
- 历史数据兼容与不匹配计数已支持（人体/弓箭/框）。

5. 自动预标注（模块2增强）
- Halpe26 ONNX 人体预标注可用。
- RTMO ONNX 弓箭预标注可用。
- 已统一阈值接口为 `confidence + iou`（UI 与调用一致）。
- 支持“全部图片自动标注”。

6. 模块3（训练模型）v0.3（本轮重点）
- 训练页改为左右布局：
  - 左侧：训练配置 + 训练日志
  - 右侧：loss/precision 实时曲线绘制
- 训练页移除“可编辑项目目录”，改为固定显示当前项目目录。
- 训练页新增 GUI 字段：
  - `skeleton_info`
  - `joint_weights`
  - `sigmas`
- 训练配置保留单类别输入 `category_name`，与单目标训练一致。

7. 训练配置与稳定性修复（关键）
- 修复你遇到的配置继承报错：
  - `val_evaluator/test_evaluator` 类型冲突（dict/list）已改为按基配置动态生成。
  - `metainfo/metafile` 冲突与子字段合并冲突已处理（含 `_delete_=True` 策略）。
- 训练子进程环境兼容性已增强（Windows 编码与环境变量处理），当前 GUI 可正常拉起训练。
- `RTMO-s` 已默认绑定官方预训练权重：
  - `rtmo-s_8xb32-600e_coco-640x640-8db55a59_20231211.pth`
  - 切换模型为 `RTMO-s` 且初始化模式为“加载预训练权重”时自动填充。
- 已验证可生成并通过 `mmengine.Config.fromfile(...)` 的训练配置：
  - `RTMPose-X(HALPE26)`
  - `RTMO-s`

## 进行中

1. 训练曲线解析精度
- 当前已可从日志实时提取并绘制 loss/precision。
- 不同日志格式下 mAP/precision/recall 的鲁棒解析仍在持续优化。

2. 标签 JSON 标准化
- 当前支持多种导入形态。
- 下一步将固定并文档化统一 schema（减少歧义）。

## 未完成

1. 模块4（应用模型）
- 实时摄像头推理、FPS 面板、在线可视化尚未接入。

2. 模块5（导出数据）
- `raw_2d.csv / filtered_2d.csv` 导出尚未接入。
- ONNX 导出统一流程与推理回归校验尚未接入。

3. 训练结果管理
- best/latest 权重 UI 管理、训练后可视化验证面板尚未完成。

---

## 2. 里程碑状态

## 里程碑A：导入 + 标注闭环
状态：`已完成（增强版）`

验收结论：
1. 可导入项目与图片列表。
2. 可进行关键点与矩形双类型标注。
3. 支持标签导入/新增并落盘。
4. 支持自动预标注与批量自动标注。
5. 支持自动保存与坐标状态显示。

## 里程碑B：训练闭环
状态：`进行中（v0.3，已可稳定启动训练）`

已达成：
1. GUI 参数化配置训练任务与单目标类别。
2. MMPose 数据构建（train/val/test）。
3. 训练配置生成与命令填充。
4. 子进程训练启动与日志回显。
5. 训练页实时曲线（loss/precision）显示。
6. 关键配置继承错误已修复。
7. `RTMO-s` 默认预训练权重自动填充已完成。

待完成：
1. 指标卡片（mAP/precision/recall）结构化显示。
2. 训练产物管理（best/latest 可视化管理）。
3. 训练完成后可视化验证入口。

## 里程碑C：应用 + 导出
状态：`未开始`

目标：
1. 摄像头实时推理与关键点绘制。
2. 导出 CSV（原始/滤波）。
3. 导出 ONNX + 推理验证入口。

---

## 3. 下一步任务（按优先级）

1. 稳定训练闭环（P0）
- 完成 mAP/precision/recall 指标解析与面板化展示。
- 增加 epoch/iter/ETA 明确显示。
- 增加训练结束状态总结与产物链接。

2. 标注 schema 固化（P0）
- 固化标签 JSON 规范（关键点集合名、id、name、检测类名）。
- 增加导入校验提示（重复名、非法 id、空集合等）。

3. 数据导出增强（P1）
- 增加 bbox + keypoint 的统一导出（JSON/CSV）。
- 明确“仅当前目标训练”与“全标注保留”策略。

4. 应用模块首版（P1）
- 接入实时摄像头推理。
- 叠加关键点、置信度、基础 FPS 指标。

---

## 4. 风险与注意事项

1. 环境约束
- 训练要求 `mmpose==1.3.2`。
- GUI 运行需在包含 `PySide6` 的虚拟环境内。

2. 配置兼容
- 不同基配置的 evaluator/container 结构可能不同，需保持动态兼容策略。

3. 数据一致性
- 历史标注不做破坏性迁移；不匹配项仅告警并保留原数据。

---

## 5. 本周目标

1. 训练模块达到“可观测 + 可复现”
- 训练配置可稳定生成
- 训练过程可视化
- 产物可定位

2. 标注模块达到“标签可配置 + 双类型可用”
- 关键点/矩形标注流程完整
- 自动保存与状态反馈稳定

3. 为模块4/5打基础
- 统一数据结构与导出协议，减少后续返工

