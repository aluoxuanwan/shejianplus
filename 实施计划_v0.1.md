# 射箭Plus 实施计划（进度对齐版）

更新时间：2026-02-14  
项目状态：`2D MVP` 已进入“训练模块 v0.2（数据与配置）已打通”阶段

## 1. 当前进展总览

## 已完成

1. 工程骨架与运行入口
- 已完成 `run.py` + `src/archery_plus` 包结构。
- 已完成主窗口与 5 模块页面框架。

2. 模块1（导入素材）
- 已接入 OpenCV 抽帧：支持 MP4/AVI、抽帧间隔 1~30。
- 已接入项目目录初始化与日志反馈。

3. 模块2（标注图片）
- 已完成图片列表、画布点选/拖拽/删除。
- 已完成编辑目标切换：`弓箭关键点` / `人体关键点`。
- 已完成自动保存、自动跳图与手工保存。
- 已完成标注保存与回读：`annotations/manual_annotations.json`。

4. 自动预标注（模块2增强）
- 已接入 Halpe26 ONNX 人体预标注（支持置信度/NMS）。
- 已接入 RTMO ONNX 弓箭预标注（支持置信度/NMS）。
- 已支持“全部图片自动标注”。

5. 标注数据结构
- 已升级为每图结构：
  - `{"human_keypoints": [...], "archery_keypoints": [...]}`
- 已兼容旧版仅列表格式读取。

6. 模块3（训练模型）v0.2
- 已接入 MMPose 数据构建器：
  - 从 `manual_annotations.json` 生成 COCO Keypoint 标注。
  - 人体/弓箭分别产出 `train/val/test`（默认 `8:1:1`）。
  - 输出目录：`output/mmpose_dataset_v1/annotations/*.json`。
  - 产出 `dataset_meta.json`（类别、关键点、样本统计）。
- 已接入训练配置生成器：
  - 支持模型选择（默认 `RTMO-s`）、任务类型（关键点检测）。
  - 支持初始化方式（预训练 / 从头）。
  - 支持超参数：`epochs/batch_size/num_workers/image_size/lr/optimizer/scheduler/gpus`。
  - 自动生成训练配置文件：`output/train_configs/*.py`。
  - 自动生成训练命令并可一键填充。
- 已接入配置预设：
  - GUI 支持“保存预设 / 加载预设”（JSON）。

## 进行中

1. 训练过程可视化
- 日志实时显示已可用。
- loss 曲线、训练进度条、指标卡片（mAP/precision/recall）尚在下一阶段。

2. 训练配置稳定性
- 针对 RTMO 自定义关键点元信息的进一步验证与调参。

## 未完成

1. 模块3 训练监控完整面板（曲线 + 指标）尚未接入。  
2. 模块4 实时摄像头推理尚未接入。  
3. 模块5 CSV 导出（raw/filtered）尚未接入。  
4. 训练完成后的“一键 ONNX 导出 + 可视化验证”尚未接入。

---

## 2. 里程碑状态

## 里程碑A：导入 + 标注闭环
状态：`已完成（增强版）`

验收结论：
1. 已可从视频抽帧到图片目录。
2. 已可逐图标注、保存、回读。
3. 已支持人体/弓箭双模型自动预标注。
4. 已支持整批图片自动标注。

## 里程碑B：训练闭环
状态：`进行中（v0.2 已完成）`

已达成：
1. GUI 参数化配置训练任务。
2. 生成 MMPose 可训练数据（COCO + train/val/test）。
3. 生成训练配置文件与训练命令。
4. 子进程启动训练与日志回显。

待完成：
1. loss 曲线与训练进度可视化。
2. mAP/precision/recall 指标面板。
3. best/latest 模型管理与训练结束可视化验证。

## 里程碑C：应用 + 导出
状态：`未开始`

目标：
1. 摄像头实时推理显示关键点与 FPS。
2. 导出 `raw_2d.csv` / `filtered_2d.csv`。
3. 导出 ONNX 并提供推理校验入口。

---

## 3. 下一步任务（按优先级）

1. 模块3 训练监控增强
- 增加 loss/lr 曲线绘制。
- 解析并显示 mAP、precision、recall。
- 增加 epoch/iter 进度显示。

2. 模块3 训练产物管理
- 展示 best/latest 权重路径。
- 增加训练完成后的关键样本可视化验证。

3. 模块4 实时推理首版
- 单摄像头 + 单模型推理与关键点绘制。

4. 模块5 导出首版
- 导出关键点原始坐标与置信度 CSV。

---

## 4. 技术债与注意事项

1. 编码一致性
- 所有 `.py/.md/.json` 统一 UTF-8。

2. 数据规范
- 继续维持 `human_keypoints` / `archery_keypoints` 双层结构。

3. 训练兼容性
- Windows 下训练需保持 `HOME/USERPROFILE` 环境变量完整。
- 如出现内存/页面文件错误，需提高系统虚拟内存。

---

## 5. 两周目标

1. 达到“可视化训练”里程碑
- 训练启动 -> 实时曲线/指标 -> 权重产出 -> 可视化验证。

2. 达到“可推理”里程碑
- 摄像头实时显示关键点 + 基础 CSV 导出。

3. 保持策略
- 继续优先打通端到端，再做性能和 UI 精修。
