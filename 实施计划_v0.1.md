# 射箭Plus 实施计划（实时同步版）

更新时间：2026-02-24  
项目状态：`2D MVP` 已进入“应用模块首版可用 + 导出模块 v0.1 落地”阶段

## 1. 当前进展总览

## 已完成

1. 工程与主界面
- `run.py` + `src/archery_plus` 工程骨架可运行。
- 主窗口 5 模块框架稳定：导入、标注、训练、应用、导出。

2. 模块1（导入素材）
- 项目目录初始化、抽帧入口、日志反馈可用。
- 项目目录结构初始化逻辑可复用。

3. 模块2（标注图片）
- 标注页已改为“标签驱动”，支持标签 JSON 导入、添加关键点标签、添加矩形标签。
- 支持关键点标注（人体/弓箭）与矩形框标注（bbox）。
- 点与框都支持新增/拖拽/右键删除。
- 自动保存开关可用，新增/删除标注后可自动保存。
- 左下角已显示鼠标像素坐标与图片序号：`(X:,Y:)[图片名称:当前/总数]`。
- 自动预标注参数统一为 `confidence + IoU`。

4. 标注一致性修复（本轮关键）
- 人体自动预标注输出已显式携带 `id`。
- 去除会打乱关键点语义顺序的人体点级重排逻辑，避免 `nose/hip` 等错位映射。
- 标注页映射逻辑已改为“优先按 `id` 对齐名称”，缺失时才回退到名称/索引。
- 标注画布支持按 `id` 与当前标签集合一致显示/导出。

5. 标注数据结构
- 每图结构已支持：
  - `human_keypoints`
  - `archery_keypoints`
  - `bboxes`
- 标签 schema 文件：`<project>/annotations/label_schema.json`。
- 历史数据不做破坏性迁移，不匹配只告警并保留。

6. 自动预标注（模块2增强）
- Halpe26 ONNX 人体预标注可用。
- RTMO ONNX 弓箭预标注可用。
- 支持“全部图片自动标注”。

7. 模块3（训练模型）
- 训练页保持左右布局，左侧配置+日志，右侧训练状态与曲线。
- 训练区已调整为左右 `5:5` 分割。
- 训练参数已整合为统一“训练参数”区，并带提示说明。
- `input_size` 已改为可编辑 `w/h`：
  - HALPE26 模型默认填充 `288x384`，但允许手动修改。
  - 其他模型默认 `640x640`。
- 训练配置生成已支持非正方形输入尺寸（`input_width/input_height`）。

8. 训练配置与稳定性
- 修复 evaluator 类型冲突、metainfo 合并冲突等配置继承问题。
- Windows 训练子进程环境处理增强（编码/环境变量兼容）。
- `RTMO-s` 自动填充官方预训练权重：
  - `rtmo-s_8xb32-600e_coco-640x640-8db55a59_20231211.pth`
- 训练状态栏已接入：状态、epoch/iter/ETA、当前指标、最佳指标与结束摘要。

9. 模块4（应用模型）首版增强（本轮重点）
- 输入源支持视频文件实时推理（按视频 FPS 播放）。
- 支持推理目标切换：
  - 弓箭关键点（RTMO）
  - 人体关键点（RTMPose-HALPE26）
  - 双模型同时推理（弓箭 + 人体）
- 应用页支持实时滤波：
  - `One Euro`（一欧元滤波器，主用）
  - 双向四阶巴特沃斯（滚动窗口近实时实现）
- 滤波参数可调：
  - `min_cutoff / beta / d_cutoff`
  - 巴特沃斯 `Hz / 窗口秒数`
- 支持弓长像素-厘米换算（UP-DOWN 标尺）与比例冻结策略。
- 实时状态显示：
  - `推理FPS`
  - `视频FPS`
  - 丢帧提示
- 参数分析面板已接入：
  - 关键点瞬时速度（默认按视频 FPS/帧号计算）
  - 关键点轨迹回看
  - 两点/三点/四点角度计算
- 双模型显示体验已优化：
  - 画布分色（弓箭橙色 / 人体蓝色）
  - 右侧表格按“弓箭关键点 / 人体关键点”分组

10. 模块5（导出数据）v0.2（持续推进）
- 导出页已从占位页升级为可操作 UI。
- 支持从项目标注导出到 `<project>/output/exports/<时间戳>/`：
  - `raw_2d.csv`（关键点长表）
  - `filtered_2d.csv`（v0.1 占位导出，当前与 raw 同数据并标记 `filter_method`）
  - `bbox_2d.csv`（矩形框）
  - `annotations_export.json`（统一导出包）
  - `label_schema_export.json`（标签结构）
- 导出日志、错误提示、文件列表反馈已接入。
- 应用模块内已支持“推理时序 CSV 导出”（当前会话）：
  - `raw_2d_timeseries.csv`
  - `filtered_2d_timeseries.csv`
  - `session_meta.json`

## 进行中

1. 指标解析鲁棒性
- 已支持从日志提取 loss/mAP/precision/recall。
- 仍需进一步适配更多日志格式。

2. 标签 JSON 标准化文档
- 当前导入兼容较灵活。
- 下一步固化并文档化标准 schema。

## 未完成

1. 模块4（应用模型）
- 实时摄像头输入尚未接入（当前为视频文件实时推理首版）。
- 画布图例、分面板结果视图仍可继续优化。

2. 模块5（导出数据）
- 导出模块已完成 v0.2（标注导出 + 应用内推理时序导出）。
- 尚未接入：
  - 导出页统一管理“推理时序导出”任务（当前入口在应用模块）
  - 叠加视频导出
  - ONNX 导出统一流程与推理回归校验

3. 训练结果管理
- best/latest 权重可视化管理。
- 训练后验证入口与样例回放面板。

---

## 2. 里程碑状态

## 里程碑A：导入 + 标注闭环
状态：`已完成（增强版）`

验收结论：
1. 可导入项目与图片列表。
2. 可进行关键点与矩形双类型标注。
3. 支持标签导入/新增并落盘。
4. 支持自动预标注与批量自动标注。
5. 已完成关键点错位映射修正（按 id 对齐）。

## 里程碑B：训练闭环
状态：`进行中（v0.3，已可稳定启动训练）`

已达成：
1. GUI 参数化配置训练任务与单目标类别。
2. MMPose 数据构建（train/val/test）。
3. 训练配置生成与命令填充。
4. 子进程训练启动与日志回显。
5. 实时曲线（loss/precision）与训练状态指标面板。
6. 支持可编辑 `input_size(w/h)`，HALPE26 默认值自动带出。
7. `RTMO-s` 默认预训练权重自动填充。

待完成：
1. 指标解析鲁棒性继续优化。
2. 训练产物管理（best/latest 可视化）。
3. 训练完成后可视化验证入口。

## 里程碑C：应用 + 导出
状态：`进行中（模块4首版可用 + 模块5 v0.1）`

目标：
1. 摄像头实时推理与关键点绘制（待完成）。
2. 导出 CSV（原始/滤波）基础能力（已完成标注导出；待接入推理时序导出）。
3. 导出 ONNX + 推理验证入口（待完成）。

---

## 3. 下一步任务（按优先级）

1. 稳定训练闭环（P0）
- 提升日志解析兼容面，减少不同模型日志格式下的指标遗漏。
- 增加训练结果文件快捷入口（best/latest、配置、日志）。

2. 标注 schema 固化（P0）
- 发布统一标签 JSON 规范（关键点集合、检测标签、id/name 规则）。
- 增加导入校验与错误提示（重复名、非法 id、空集合）。

3. 导出增强（P1）
- 将“推理结果时序导出”统一收敛到导出页（当前已在应用模块可用）。
- 增加导出范围与目标筛选（弓箭/人体/双模型）。
- 增加叠加视频导出与导出配置记录。

4. 应用模块增强（P1）
- 接入实时摄像头推理。
- 增加画布图例与更清晰的双模型结果视图（双表/分页）。

---

## 4. 风险与注意事项

1. 环境约束
- 训练要求 `mmpose==1.3.2`。
- GUI 运行需在包含 `PySide6` 的虚拟环境内。

2. 配置兼容
- 不同基配置的 evaluator/container 结构可能不同，需保持动态兼容策略。

3. 数据一致性
- 历史标注不做破坏性迁移；不匹配项仅告警并保留原数据。

---

## 5. 本周目标

1. 训练模块达到“可观测 + 可复现”
- 配置稳定生成
- 训练过程可视化
- 产物可定位

2. 标注模块达到“标签可配置 + 双类型可用 + 语义一致”
- 关键点/矩形流程完整
- 自动保存稳定
- 关键点 id 与标签映射一致

3. 推进模块4/5进入可交付形态
- 应用模块：双模型实时分析稳定化
- 导出模块：从标注导出扩展到推理时序导出
